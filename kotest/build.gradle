import app.ekaralamov.gradle.BaseKotlinPlugin

apply plugin: 'com.android.library'

new BaseKotlinPlugin().apply(project)

dependencies {
    api 'io.kotest:kotest-runner-junit5-jvm:4.0.2'
    api 'io.mockk:mockk:1.9.3'
    api 'org.jetbrains.kotlinx:kotlinx-coroutines-test:1.3.5'
}
